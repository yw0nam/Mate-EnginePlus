# Fixture for EmotionMapperTests. Not loaded directly by the test — the test
# inlines the YAML string in [SetUp]. This file exists as a reference for
# what the production format looks like in a passing test.
😊:
  - duration: 0.3
    targets:
      happy: 1.0
😢:
  - duration: 0.4
    targets:
      sad: 1.0
